[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "imitune-feedback-downloader"
version = "1.1.0"
description = "Download, consolidate, and analyse ThatSoundLikeMe feedback from Vercel Blob Storage"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
    {name = "ImiTune Team"}
]
keywords = ["vercel", "blob", "feedback", "imitune"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "requests>=2.32.4",
    "tqdm>=4.67.1",
]

[project.scripts]
download-feedback = "download_feedback:main"

[project.urls]
Homepage = "https://thatsoundslike.me"
Repository = "https://github.com/imitune/imitune-frontend"

[tool.hatch.build.targets.wheel]
packages = ["."]
