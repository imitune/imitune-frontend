# ImiTune Frontend Monorepo

This repo contains the web frontend in `web/`.

- App README and setup instructions: `web/README.md`
- Start the dev server:

  ```bash
  cd web
  npm install
  npm run dev
  ```